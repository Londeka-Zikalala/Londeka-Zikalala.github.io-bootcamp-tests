describe("regCheck", function()){
    it("should return registration numbers that start with a given string value and end with another given string value",
    function(arg1,arg2){ 
        assert.equal("arg1.endsWith("arg2"), true);
        assert.equal("arg1.endsWith("arg2"), false);
    }
    )
}
)